import { defineMeta } from '@platform';

/** Model gallery (a development preview, `?game=gallery`): glTF models as props and figures, and block shapes. */
export default defineMeta({
  id: 'gallery',
  title: 'Model gallery',
  tagline: 'glTF models: props and figures',
});
