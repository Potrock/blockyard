import { defineMeta } from '@platform';

/** Dev preview: the Bed Wars skins and item sprites (`?game=bedwars-art`; never in production builds). */
export default defineMeta({
  id: 'bedwars-art',
  title: 'Bed Wars art (dev preview)',
});
