import { defineMeta } from '@platform';

/** Dev preview: the Bed Wars map in the void (`?game=bedwars-map`; never in production builds). */
export default defineMeta({
  id: 'bedwars-map',
  title: 'Bed Wars map (dev preview)',
});
