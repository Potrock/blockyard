import { defineMeta } from '@platform';

/** Dev preview: the capital ship in place (`?game=drydock`; never in production builds). */
export default defineMeta({
  id: 'drydock',
  title: 'Drydock (dev preview)',
  tagline: 'The capital ship in place',
});
