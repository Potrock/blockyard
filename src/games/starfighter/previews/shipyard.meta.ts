import { defineMeta } from '@platform';

/** Dev preview for ship builds (`?game=shipyard`; never in production builds). */
export default defineMeta({
  id: 'shipyard',
  title: 'Shipyard (dev preview)',
  tagline: 'Ship builds, full size',
});
