import { defineMeta } from '@platform';

/** Dev preview: the gun models on pedestals (`?game=cob-guns`; never in production builds). */
export default defineMeta({
  id: 'cob-guns',
  title: 'Call of Blocky: guns',
});
