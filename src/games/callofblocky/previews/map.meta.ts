import { defineMeta } from '@platform';

/** Dev preview: Jackrabbit Lane on its own (`?game=cob-map`; never in production builds). */
export default defineMeta({
  id: 'cob-map',
  title: 'Call of Blocky: map',
});
