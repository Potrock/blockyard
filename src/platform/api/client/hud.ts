/**
 * The HUD, for a game's client code and kits: layers of its own (DOM: the game's client code is
 * trusted), and the platform's panels (see `hud.gunner()`, `hud.throwables()`).
 *
 * (Phase 2: the HUD agent fills this in.)
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ClientHud {}
